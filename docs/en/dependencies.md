# Dependencies

ncorn has minimal dependencies:

- `httptools` - HTTP parsing
- `rich` - Colored logging

## Installation

```bash
pip install ncorn
```

## Development Dependencies

```bash
pip install -e ".[dev]"
```
