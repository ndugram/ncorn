# Зависимости

У ncorn минимальные зависимости:

- `httptools` - HTTP парсинг
- `rich` - Цветное логирование

## Установка

```bash
pip install ncorn
```

## Зависимости для разработки

```bash
pip install -e ".[dev]"
```
